@php $label=ucfirst($section); @endphp
<x-app-layout :title="$label">
<x-page-title :title="$label" description="Manage access and workspace security records."><x-button variant="secondary" size="sm" onclick="window.print()">Print</x-button>@can($section === 'users' ? 'users.create' : ($section === 'roles' ? 'roles.manage' : 'permissions.manage'))<x-button :href="route('administration.create',$section)">Create {{ Str::singular($label) }}</x-button>@endcan</x-page-title>
<x-card><div x-data="{query:'', selected:[]}" class="min-w-0 max-w-full space-y-4"><div class="flex min-w-0 flex-wrap items-center gap-3"><label class="relative min-w-0 flex-[1_1_14rem]"><span class="sr-only">Search</span><input x-model="query" class="app-field py-2 pl-9" placeholder="Search {{ strtolower($label) }}…"><x-icon name="search" class="absolute left-3 top-2.5 h-4 w-4 text-slate-400"/></label><x-button variant="secondary" size="sm">Export CSV</x-button><span x-show="selected.length" x-cloak class="text-xs font-medium text-primary" x-text="selected.length+' selected'"></span></div>
<x-table caption="{{ $label }} administration records" class="min-w-[680px]"><thead class="border-y border-slate-200 bg-[#fffaf6] text-[11px] font-medium uppercase tracking-wider text-slate-500 dark:border-slate-800 dark:bg-slate-800/60"><tr><th class="w-10 p-3"><input type="checkbox" class="border-slate-300 text-primary focus:ring-accent"></th><th class="p-3">Name</th><th class="p-3">Scope / email</th><th class="p-3">Status</th><th class="p-3">Updated</th><th class="p-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">
@forelse($items as $item)<tr x-show="query==='' || @js(strtolower(($item->name ?? '').' '.($item->email ?? ''))).includes(query.toLowerCase())" class="hover:bg-[#fffaf6] dark:hover:bg-slate-800/40"><td class="p-3"><input x-model="selected" value="{{ $item->id }}" type="checkbox" class="border-slate-300 text-primary focus:ring-accent"></td><td class="p-3 font-medium text-slate-900 dark:text-white">{{ $item->name }}</td><td class="p-3 text-slate-500">{{ $item->email ?? ($item->guard_name ?? 'web') }}</td><td class="p-3"><x-badge tone="green">Active</x-badge></td><td class="p-3 text-slate-500">{{ optional($item->updated_at)->diffForHumans() ?? 'Recently' }}</td><td class="p-3 text-right"><a class="font-medium text-primary hover:text-accent hover:underline" href="{{ route('administration.show',[$section,$item->id]) }}">View</a></td></tr>@empty<tr><td colspan="6"><x-empty-state :title="'No '.strtolower($label).' found'" description="Create the first record to begin configuring access."><x-button :href="route('administration.create',$section)">Create record</x-button></x-empty-state></td></tr>@endforelse
</tbody></x-table><div class="flex flex-wrap items-center justify-between gap-2 border-t border-slate-100 pt-4 text-xs text-slate-500 dark:border-slate-800"><span>Showing {{ $items->count() }} records</span><span>Page 1 of 1</span></div></div></x-card>
</x-app-layout>
