@props(['title','description'=>null,'eyebrow'=>null])<header class="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div>@if($eyebrow)<p class="mb-1.5 text-[11px] font-medium uppercase tracking-[.16em] text-accent">{{ $eyebrow }}</p>@endif<h1 class="text-2xl font-semibold tracking-tight text-primary dark:text-white">{{ $title }}</h1>@if($description)<p class="mt-1.5 max-w-2xl text-sm font-normal text-slate-500 dark:text-slate-400">{{ $description }}</p>@endif</div>@if(trim($slot))<div class="flex flex-wrap gap-2">{{ $slot }}</div>@endif</header>
