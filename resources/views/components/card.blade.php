@props(['title'=>null,'subtitle'=>null])<section {{ $attributes->merge(['class'=>'app-panel min-w-0 max-w-full']) }}>@if($title)<header class="border-b border-[#eee3dc] px-5 py-4 dark:border-slate-800"><h2 class="text-sm font-semibold text-primary dark:text-white">{{ $title }}</h2>@if($subtitle)<p class="mt-1 text-xs font-normal text-slate-500">{{ $subtitle }}</p>@endif</header>@endif<div class="min-w-0 max-w-full p-5">{{ $slot }}</div></section>
